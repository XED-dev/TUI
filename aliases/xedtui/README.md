# xedtui

> This is an alias package. Please install **[xed-tui](https://pypi.org/project/xed-tui/)** instead.

```bash
pip install xed-tui
# or
uv tool install xed-tui
```

Installing `xedtui` will automatically install `xed-tui` as a dependency.
